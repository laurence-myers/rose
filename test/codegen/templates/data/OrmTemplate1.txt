const FooTableAllColumns = {
	id: QFooTable.id,
};
const FooTableDefaultQueries = {
	getOne: (function getOne() {
		interface Params {
			id: number;
		}

		const P = new ParamsWrapper<Params>();
		return select<FooTableAllColumns, Params>(FooTableAllColumns).where(QFooTable.id.eq(P.get((p) => p.id))).prepare();
	})(),
	updateOne: function updateOne(updates: PartialTableColumns<TFooTable>) {
		interface Params {
			id: number;
		}

		const P = new ParamsWrapper<Params>();
		return updateFromObject<TFooTable, Params>(QFooTable, updates).where(QFooTable.id.eq(P.get((p) => p.id))).prepare();
	},
};
