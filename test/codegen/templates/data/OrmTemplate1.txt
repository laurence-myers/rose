const FooTableAllColumns = {
	id: QFooTable.id
};
const FooTableDefaultQueries = {
	getOne: (function () {
		interface Params {
			id: number;
		}

		const P = new ParamsWrapper<Params>();
		return select(FooTableAllColumns).where(QFooTable.id.eq(P.get((p) => p.id)));
	})()
};
